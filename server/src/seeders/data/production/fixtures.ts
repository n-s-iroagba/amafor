import { FixtureAttributes } from '@models/Fixture';

export const productionFixtures: FixtureAttributes[] = [
  // Add essential production data for Fixture here
  // Only include critical data needed for production
];
