#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🎯 PRE-COMMIT HOOK IS RUNNING!"
echo "Current directory: $(pwd)"
echo "User: $(whoami)"
echo "Timestamp: $(date)"

# Always exit with 0 (success) for testing
exit 0
