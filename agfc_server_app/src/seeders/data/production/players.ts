import { PlayerAttributes } from '@models/Player';

export const productionPlayers: PlayerAttributes[] = [
  // Add essential production data for Player here
  // Only include critical data needed for production
];
