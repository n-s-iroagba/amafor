import { DonationAttributes } from '@models/Donation';

export const productionDonations: DonationAttributes[] = [
  // Add essential production data for Donation here
  // Only include critical data needed for production
];
